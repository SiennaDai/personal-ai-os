interface:
  display_name: "Systematic Debugging"
  short_description: "Find root causes before proposing or applying fixes"
  default_prompt: "Use $systematic-debugging to reproduce this failure, isolate its root cause, and recommend the next step."
