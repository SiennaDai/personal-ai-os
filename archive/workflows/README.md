# Archived Workflows

This directory preserves standalone Workflow documents from architectures before v1. Workflows are design history, not separately invoked runtime components. Their meaningful orchestration behavior belongs in the corresponding Agent definition.
